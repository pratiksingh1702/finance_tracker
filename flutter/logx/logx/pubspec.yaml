name: logx
version: 1.0.0
description: Production-grade caller-aware structured logging with global access
homepage: https://github.com/yourorg/logx

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  shelf: ^1.4.0
  shelf_web_socket: ^1.0.4
  web_socket_channel: ^2.4.0
  path_provider: ^2.1.0
  synchronized: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0